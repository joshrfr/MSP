import React from 'react';
import AppRedirect from '../components/AppRedirect';

const EnterpriseDiscovery = () => {
  return (
    <AppRedirect
      title="Enterprise Plans on Our Portal"
      description="Our ENTERPRISE tier with custom SLAs and dedicated support is now available through app.tttmsp.com. Schedule your discovery call and get a custom quote."
    />
  );
};

export default EnterpriseDiscovery;
